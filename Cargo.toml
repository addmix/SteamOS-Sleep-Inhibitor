[package]
name = "sleep_inhibitor"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
zbus = { version = "5", features = ["tokio"] }

[profile.rpm]
inherits = "release"
